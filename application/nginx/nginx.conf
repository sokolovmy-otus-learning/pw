events {}

http {
    upstream link_service {
        server link-service:3000;
    }

    upstream redirect_service {
        server redirect-service:3001;
    }

    server {
        listen 80;

        # Обслуживание статических файлов фронтенда
        location / {
            root /usr/share/nginx/html;
            index index.html;
            try_files $uri $uri/ /index.html;
        }

        # Проксирование API-запросов к link-service
        location /api/ {
            proxy_pass http://link_service/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        # Проксирование коротких ссылок к redirect-service
        # Соответствует /AbCdEf (6 символов: буквы a-z, A-Z, цифры 0-9)
        location ~ ^/\w\w\w\w\w\w$ {
            proxy_pass http://redirect_service/$1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
    }
}